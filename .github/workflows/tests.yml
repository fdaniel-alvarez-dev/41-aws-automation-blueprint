name: tests
on:
  push:
  pull_request:

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run demo-mode tests (offline)
        env:
          TEST_MODE: demo
        run: python3 tests/run_tests.py
