# Data trust contract (minimal, practical)

This repo treats the pipeline like production:
- schema is explicit
- quality checks run in CI
- outputs are deterministic

The point is to make downstream consumers confident without slowing delivery.
